Simple driver with CLI, which blocks any device from being reached in user-mode. This means blocking a device of a pad, blocks games from using this pad.

In order to block device - find it's handle in device manager, right click, properties, details, Physical Device Object name, copy, insert into CLI, block.

Device is blocked as long as it is not unblocked or computer is reset.
